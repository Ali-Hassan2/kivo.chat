USER SENDS POST REQUEST WITH USERNAME TO BLOCK
CHECK IF REQUEST METHOD IS POST, RETURN 405 IF NOT
GET SESSION AND USER ID, RETURN 400 IF USER NOT FOUND
CONNECT TO DATABASE
PARSE AND VALIDATE USERNAME, RETURN 400 IF INVALID
FIND CURRENT USER AND TARGET USER PARALLELY
RETURN 400 IF EITHER USER NOT FOUND
CHECK IF TARGET USER IS IN FRIENDS LIST
IF YES, REMOVE TARGET USER FROM FRIENDS LIST
CHECK IF TARGET USER IS THE SAME AS CURRENT USER, RETURN 400 IF TRUE
CHECK IF TARGET USER IS ALREADY BLOCKED, RETURN 400 IF TRUE
PUSH TARGET USER ID TO BLOCKS ARRAY
SAVE CURRENT USER
RETURN SUCCESS RESPONSE
