const VERIFICATION_CODE = '/verification-code'

const AUTH = '/auth'

const KIVO_PROFILE = '/dashboard'

const PERSONAL_BOARD = '/personal-dashboard'

const NETWORK = '/connection-network'

const ABOUT_PAGE = '/about-kivo'

const CONTACT_PAGE = '/support-kivo'

const EXCLUDED_PAGES = ['/', '/auth', '/signup', '/verification-code']

export {
  VERIFICATION_CODE,
  KIVO_PROFILE,
  AUTH,
  PERSONAL_BOARD,
  EXCLUDED_PAGES,
  NETWORK,
  ABOUT_PAGE,
  CONTACT_PAGE,
}
